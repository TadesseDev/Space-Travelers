import React from 'react';
import { NavLink } from 'react-router-dom';

return (
  <nav style={navStyle}><NavLink to="/Profile" style={lstLink}>My Profile</NavLink></li>
      </ul >
    </nav >
  );
};

export default Navbar;